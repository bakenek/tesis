<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_clicking_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_clicking_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tesis">
        <activity
            android:name=".ui.dashboard.ViewActivityDashboard"
            android:exported="true">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".ActualizarUsuario"
            android:exported="true"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".Registrar"
            android:exported="true"></activity>
        <activity
            android:name=".AgregarServicio"
            android:exported="true"></activity>
        <activity
            android:name=".First_layout"
            android:theme="@style/Tesisinicio"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>